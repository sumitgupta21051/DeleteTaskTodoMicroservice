apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: delete-task-ingress

spec:
  ingressClassName: azure-application-gateway
  rules:
  - host: deltask.devops-engineer-adda.store
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: delete-task
            port: 
              number: 80
